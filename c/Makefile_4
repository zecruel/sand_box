SRC_PATH=./src/
CC=gcc
COMPILER_FLAGS = -g -c
LINKER_FLAGS = -lSDL2main -lSDL2 -lcomdlg32 -lole32 -lmingw32
INCLUDE_PATHS = -I. -I./src/ -I/usr/include/SDL2
LIBRARY_PATHS = -Llib -L/usr/lib
EXE=teste9.exe

SRC=$(wildcard $(SRC_PATH)*.c)
OBJ=$(subst ./src, ./obj, $(SRC:.c=.o))

all: $(SRC) $(EXE)

$(EXE): $(OBJ)
	$(CC) $(LINKER_FLAGS) $(OBJ) $(LINKER_FLAGS) -o $@

./obj/%.o: ./src/%.c
	$(CC) $(INCLUDE_PATHS) $(COMPILER_FLAGS) -o $@ $<

clean:
	rm -rf run $(OBJ)